# Laravel Notification

Laravel-notification is an easy to use package working with both Laravel and Lumen for sending push notification with [Firebase Cloud Messaging](https://firebase.google.com/docs/cloud-messaging/) (FCM).

## Config

## Installation

php artisan vendor:publish --tag=fcm-config

php artisan vendor:publish --tag=fcm-migrations

## Usage

### Routing

```php
```
