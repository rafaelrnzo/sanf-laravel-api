#!/usr/bin/env sh

echo "  > Build Docker image app/builder..."
docker build -t sanf/builder deployments/builder

echo "  > Build Docker image app/core..."
docker build -t sanf/core deployments/core

echo "  > Done"
