#!/usr/bin/env sh

echo "  > Stop running container..."
docker stop sanf-core
echo "  > Remove old container..."
docker rm sanf-core
echo "  > Run Docker container..."
docker run -d --name sanf-core --restart unless-stopped -v "$(pwd)":/var/app -p 4006:80 sanf/core

echo "  > Done"
