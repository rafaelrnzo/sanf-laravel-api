#!/usr/bin/env sh

echo "  > Build Docker image app/builder..."
docker build -t app/builder -f deployments/builder

echo "  > Composer package installation..."
docker run --rm -v $PWD:/var/app docker/builder composer install
