#!/usr/bin/env sh

echo "  > Stop running container..."
docker stop sanf-worker
echo "  > Remove old container..."
docker rm sanf-worker
echo "  > Run Docker container..."
docker run -d --name sanf-worker --restart unless-stopped -v "$(pwd)":/var/app sanf/worker

echo "  > Done"
